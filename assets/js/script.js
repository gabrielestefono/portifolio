const body = document.getElementsByTagName("body");

function scrollPages(event){
    const lengthPage = window.innerHeight;
    let posicaoAtual = event.pageY;
}

seila = body[0].addEventListener("wheel", scrollPages);